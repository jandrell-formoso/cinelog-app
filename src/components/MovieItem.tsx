/**
 * @format
 */

import React from 'react'
import { Text } from 'react-native-paper';

const MovieItem = ({ title, rating }: any) => {
  return (
    <>
      <Text>{title}</Text>
    </>
  )
}

export default MovieItem
